from graph import Graph
from collections import deque

def bfs(graph, s):
    Q = deque()
    
    dist = 0

    return dist

g = Graph()
g.add_edge('A', 'B')
g.add_edge('A', 'C')
g.add_edge('B', 'D')
g.add_edge('C', 'E')

print(bfs(g, 'A'))